PORT=3000

# Firebase Admin SDK (optional — enables FCM push notifications)
# Download from Firebase Console → Project Settings → Service Accounts → Generate new private key
FIREBASE_SERVICE_ACCOUNT=./firebase-adminsdk.json
