# Shared package

Canonical models / service contracts for Guru + Trainer.

Apps depend on local copies for independent `flutter run`, but this package
matches the assessment scaffolding and keeps contracts aligned.

```
shared/
├─ lib/models/
├─ lib/services/
├─ lib/utils/
└─ lib/widgets/
```
